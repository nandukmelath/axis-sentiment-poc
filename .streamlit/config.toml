# Streamlit Community Cloud config for the dashboard (frontend tier).
[theme]
base = "dark"
primaryColor = "#8A1538"        # Axis maroon

[server]
headless = true

[browser]
gatherUsageStats = false
