# Copy this into Streamlit Community Cloud -> app -> Settings -> Secrets (do NOT commit the real one).
# The dashboard bridges this into os.environ["DATABASE_URL"] at startup (see dashboard/app.py).
DATABASE_URL = "postgresql+psycopg2://USER:PASSWORD@ep-xxxx.neon.tech/axis?sslmode=require"

# Optional: password-gate the dashboard (leave unset for open access).
# AXIS_DASH_PASSWORD = "choose-a-password"
