# Slim deps for the Airflow worker image (pipeline tasks only — no dashboard/browser).
# Use this as your Astro project's requirements.txt to avoid dependency conflicts.
google-genai>=1.0.0
openai>=1.30
pydantic>=2.6
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
python-dotenv>=1.0
SQLAlchemy>=2.0
psycopg2-binary>=2.9
vaderSentiment>=3.3
requests>=2.31
# transform stage (Apache Beam — DirectRunner local; add apache-beam[gcp] for Dataflow)
apache-beam>=2.60
# fetch sources
praw>=7.7
google-play-scraper>=1.2
feedparser>=6.0
google-api-python-client>=2.100
