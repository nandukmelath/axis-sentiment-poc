.env
axis.db
*.db-journal
*.db-wal
fetch/x_state.json
.venv/
venv/
__pycache__/
*.pyc
exec_summary.md
weekly_digest.md
masked_mentions.csv
.pytest_cache/
.coverage
backups/
*.egg-info/
logs/
*.log
.streamlit/secrets.toml

# Generated data dumps — rebuildable from the DB (tools/db_report.py, tools/make_full_db_nb.py).
# These embed the scraped corpus itself, so they bloat the repo and duplicate axis.db.
db_export/
full_db.ipynb
AXIS_FULL_DB.html
AXIS_DATA_REPORT.html
.ipynb_checkpoints/
# NOTE: .github/workflows/ used to be ignored here because the gh OAuth token
# lacked the `workflow` scope. The workflows are tracked now and Path A of
# DEPLOY.md depends on them. To push workflow changes:
#   gh auth refresh -h github.com -s workflow
