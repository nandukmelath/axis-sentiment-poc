# Lean deps for the FastAPI backend service (Koyeb / Render / HF Spaces).
# The read-only API only needs db + fastapi; keeps the image small + fast to build.
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
psycopg2-binary>=2.9
pandas>=2.0
numpy>=1.26
pydantic>=2.6
python-dotenv>=1.0
